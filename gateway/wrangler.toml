# نشر بوّابة سديد عبر wrangler (اختياري — لوحة Cloudflare تكفي بلا أدوات)
#   npm i -g wrangler
#   wrangler login
#   cd gateway && wrangler deploy
#   wrangler secret put GEMINI_API_KEY     ← ألصق المفتاح هنا، لا في أي ملف
name = "sadeed-gateway"
main = "worker.js"
compatibility_date = "2026-08-01"
